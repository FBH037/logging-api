{
  "name": "ombud-devops-test",
  "version": "1.0.0",
  "description": "Stand up a containerized development environment that logs arbitrary JSON structures to a data store",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm install && node server.js"
  },
  "author": "Jonathan Griggs <jonathan.griggs@ombud.com>",
  "license": "MIT",
  "dependencies": {
    "express": "4.14.0"
  }
}
